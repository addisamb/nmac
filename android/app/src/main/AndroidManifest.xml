<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Legacy storage: inert on modern Android (WRITE grants nothing from API 29,
         READ superseded by READ_MEDIA_* from API 33) and, left unbounded, they are
         flagged by Play as broad storage access. Bound them so they only apply to
         the OS versions where they do something. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- 29, not 28: react-native-image-crop-picker declares maxSdkVersion=29 and the
         manifest merger fails on a mismatch. Matching its value avoids an override
         that would silently narrow that library's storage access on API 29. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Removed: CAMERA, RECORD_AUDIO, ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION.
         None are exercised anywhere in the app (no geolocation library is even
         installed; the image picker is only ever opened via openPicker, never
         openCamera). Declaring them forces a Play Console sensitive-permission
         declaration — location additionally triggers manual policy review that can
         block the release — and shows users a permission list the app never uses.
         Re-add individually if/when a capture or location feature ships. -->


    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <!-- Without this, FCM notifications delivered while the app is backgrounded
           or killed land in Android's auto-created "Miscellaneous" channel at
           default importance, ignoring the HIGH-importance channel (with sound and
           vibration) the app creates at startup. -->
      <!-- tools:replace is required: react-native-firebase_messaging declares this
           same meta-data with an EMPTY value, and the manifest merger fails on the
           conflict without an explicit override. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="nmoacademy-25acf"
        tools:replace="android:value" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
