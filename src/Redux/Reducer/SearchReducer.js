import ActionType from '../Action/ActionType/actionType';

const initialState = {

};

export default (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};


