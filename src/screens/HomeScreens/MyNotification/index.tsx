import {StyleSheet, Switch, TouchableOpacity, View} from 'react-native';
import React, {useCallback, useEffect, useState} from 'react';
import {MyNotificationScreenProps, SearchProps} from '../../propTypes';
import {
  BackHeader,
  CustomModal,
  CustomText,
  MainContainer,
  PrimaryButton,
  ToggleComponent,
} from '../../../components';
import {SettingScreenProps} from '../../propTypes';
import {t} from 'i18next';
import {
  FontType,
  Images,
  Metrix,
  NavigationService,
  RouteNames,
  Utills,
} from '../../../config';
import utills from '../../../config/utills';
import { RoundBorderCardComponent } from '../../../components/RoundBorderCardComponent';
export const MyNotificationScreen: React.FC<MyNotificationScreenProps> = ({}) => {

  return (
    <>
      
     
    </>
  );
};

interface MyNotificationScreenStyles {}
const styles = StyleSheet.create<MyNotificationScreenProps>({});
